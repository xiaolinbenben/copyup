# CopyUp 产品决策

本文记录 CopyUp 长期有效的产品和工程决策。操作过程、调试命令和发行流水账放在 `docs/DEVLOG.md`。

## 产品定位

- CopyUp 是基于 Clipy 的 macOS 菜单栏剪贴板工具，核心目标是提升高频复制、查找、粘贴的效率。
- 当前阶段优先服务个人自用和快速迭代，核心体验稳定后再规划正式发行和商业化。
- CopyUp 保留原项目 MIT 许可证和版权归属，不把原项目法律归属改写成 CopyUp 自有来源。

## 工程策略

- 二开优先少改代码，尽量复用原项目已经存在的设置项、数据模型、菜单能力和持久化路径。
- 产品意图优先通过默认值、文案和局部 UI 调整表达；只有原结构确实无法支撑目标体验时，才扩大代码改动范围。
- 不为了短期体验直接删除原有可配置能力；无法复用现有配置时先重新评估产品取舍。
- 菜单体验优先复用原设置项和数据模型；当原 `NSMenuItem` 无法支撑顶部切换、行内操作或大图预览时，可以用自定义 menu view 承担展示层。
- 自定义主菜单里的关键交互控件应在同一个 menu view 内直接处理点击和状态切换，不通过恢复多个菜单入口规避交互问题。

## 核心体验

- 主菜单优先展示可直接选择的剪贴板历史，减少进入文件夹后的二次点击。
- 主菜单应提供历史和收藏的顶部切换，这是 CopyUp 的关键产品路径。
- “打开主体”和“打开历史”打开同一个主菜单历史 tab；“打开收藏”打开同一个主菜单收藏 tab，不维护三套独立菜单体验。
- 快捷键设置只暴露历史和收藏两个主菜单入口，不再暴露单独的主体快捷键。
- 默认快捷键为历史 `Control + V`，收藏 `Control + B`。
- 快捷键打开主菜单时，面板应贴近当前鼠标位置；如果未来能稳定获取输入光标位置，可优先贴近输入光标。
- 菜单 UI 优化优先处理可读性、点击效率和内容预览尺寸，不优先改动底层数据模型。
- 历史保存上限继续使用原有偏好设置 `kCPYPrefMaxHistorySizeKey`，不在业务代码里硬编码固定条数。
- 历史菜单的展示数量和历史保存上限保持同一配置来源，避免出现保存很多但菜单只显示固定数量的隐式规则。
- 菜单设置页只保留仍影响新主菜单体验的配置；旧文件夹分组、旧 tooltip、小缩略图尺寸、旧菜单图标和隐藏图片开关不作为用户可见设置。
- 历史默认直接展示在主菜单中，自定义主面板按历史上限扁平展示，不以旧文件夹分组配置作为主路径。
- 历史菜单默认不展示序号，减少视觉噪音；用户仍应能通过菜单配置修改该行为。
- 数字键等效能力在旧原生菜单中表现为 `Command + 数字`；新主面板如启用，应通过行内快捷提示和自定义键盘处理实现，不依赖旧 `NSMenuItem` 展示方式。
- 主菜单图片历史使用容器全宽预览，不在图片旁显示 `(Files)` 这类文件类型占位文案。
- 主菜单始终显示图片预览，不为主流程提供隐藏图片的开关。
- 主菜单图片预览高度上限为内容宽度的 75%，超过时裁切显示而不是把面板撑得过高。
- 主菜单文本历史使用更大的字号，但标题内容继续复用原菜单的字符数配置，超过配置长度才追加 `...`。
- 主菜单每次打开都应显示为完整面板；内容溢出时由面板内部列表滚动，不依赖系统菜单底部箭头翻页。
- 主菜单每次打开历史 tab 时默认定位到最新剪贴板记录，不默认停留在最老记录。

## 收藏

- “收藏”是 CopyUp 里的用户手动保存内容，不使用 `Pin` 或“快捷短语”作为主要产品概念。
- 收藏第一阶段复用现有 `SnippetFolder` 和 `Snippet` 模型，不新增独立 favorites 表。
- 主菜单收藏 tab 扁平展示启用收藏内容，不按文件夹二级展开。
- 历史文本项可以通过行内星标写入收藏；图片收藏暂不支持。
- 面向用户的主流程文案从 `Snippet / 片断` 调整为 `Favorite / 收藏`，内部类型名和持久化 key 暂时保持不变。

## 发行

- CopyUp 的正式 bundle id 使用 `tech.beisi.copyup`，Debug 使用 `tech.beisi.copyup.debug`。
- 官网和更新相关域名使用 `copyup.beisi.tech`。
- 官网分发使用 Developer ID 签名和 notarization，避免 Apple Silicon 用户遇到 Rosetta 或 Gatekeeper 阻断。
- Apple Silicon 是必须支持的发行架构；如继续支持 Intel Mac，优先产出 Universal app。
- Sparkle 只服务官网分发渠道；如果未来只走 Mac App Store，应重新评估是否移除 Sparkle。

## 第三方服务

- Firebase 当前只作为可选崩溃上报能力存在，不作为 CopyUp 核心功能依赖。
- 没有 CopyUp 自有 Firebase 配置时，缺失 `GoogleService-Info.plist` 不应阻塞本地构建。
- 正式发行前需要决定保留 Firebase 还是移除相关依赖；保留时必须使用 CopyUp 自己的 Firebase 项目。
