# CopyUp TODO

## 目标

把当前 CopyUp 改造成一个更适合日常高频使用的剪贴板工具。优先解决操作路径长、菜单层级深、内容预览太小，以及 Apple Silicon 发行体验差的问题。

## 核心体验优化

- [ ] 把粘贴选择从“两次点击”优化成“一次点击”。
  - 当前问题：历史记录在 `numberOfItemsPlaceInline = 0` 时全部藏在文件夹里。
  - 第一版简单方案：调整默认菜单布局，让最近的历史记录直接显示在一级菜单。
  - 后续再补：增加偏好设置，让用户决定一级菜单直接显示多少条最近记录。

- [ ] 把现有片段功能改造成“收藏”。
  - 底层继续复用现有 `SnippetFolder` 和 `Snippet` 数据模型。
  - 第一版不新建独立的 favorites 表。
  - 产品概念统一叫“收藏”。
  - 把 snippets 视为用户手动管理的收藏项。
  - 收藏项应该尽量显示在一级菜单里，方便快速选择。
  - 增加一个简单动作：把某条历史记录保存为收藏。
  - 主流程里影响用户理解的 “Snippet / 片断” 文案逐步改成 “Favorite / 收藏”。

- [ ] 提升子菜单内容可读性。
  - 放大历史记录和收藏项的菜单文字。
  - 放大图片、文件的预览尺寸。
  - 保持菜单布局稳定，避免预览变大后菜单变得难用。

- [ ] 优化文件和图片历史记录展示。
  - 对复制的图片和文件显示更大的缩略图。
  - 文件项应该更容易识别，不依赖悬停提示或打开深层菜单。

## 发行体验

- [ ] 确保本地构建在 Apple Silicon 上原生运行。
  - 原 Clipy 官方发行版是纯 x86_64，会触发 Rosetta。
  - CopyUp 至少要提供 arm64 原生版本。

- [ ] 规划发行打包流程。
  - 官网分发需要签名和 notarization。
  - 如果还支持 Intel Mac，优先产出 arm64 + x86_64 universal app。

## 后续决策

- [ ] 决定是否保留 Firebase。
- [ ] 决定官网更新是否继续使用 Sparkle。
- [ ] 明确免费版和 Pro 版功能边界。
- [ ] 核心功能稳定后，再统一处理图标、隐私政策细节和更新发布页。
